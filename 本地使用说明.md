# 晚笺 · 微信晚间复盘

本项目基于 https://github.com/Tina2088/wechat-group-report 扩展，保留上游代码和许可证；新增功能位于 `workbench/`。没有把真实微信数据作为示例打包。

## 每天晚上怎么用

### 2026-09-21：分群故事与精简报告

- 打开后默认进入“群聊故事”。报告分为“今日重点”“群聊故事”“与我相关”“可能需要回应”“数据范围”。
- 点击群名卡片，进入该群的故事线、最多 3 个主要话题、结论与未决问题，以及本群需要回应的事项。私聊也可独立查看。
- 故事按“起因—讨论推进—最后结果”复述真实发生的事，不补写心理、对白或不存在的共识。AI 分析失败或仅有部分结果会明确标记。
- “与我相关”优先列私聊、群内 @本人、引用本人消息、本人承诺和有依据的上下文关联。“可能相关”单列，不再把全群的任务混作你的工作。
- “我的事项”用于标记处理状态，默认只显示与你相关的条目；可切换到“仅可能相关”。
- “重新整理已读记录”会用当前模型重新分析上次成功读取的时间段，无需再次读取微信。“读取并整理”仍会读取所选最新时间范围。
- 导出时一次生成 HTML、PNG 和 Markdown。PNG / Markdown 为重点版，最多 8 项个人事项和 5 个群的故事重点；完整分群内容在 HTML 内查看。不是把所有消息和候选条目重复印成长图。
- HTML 无外部依赖，可直接离线打开。首页只绘制少量重点，群列表和事项每页 12 条，群详情与原文依据按点击显示；不会一次创建数千条原文节点。
- 默认导出不附带完整聊天记录。需要完整 JSON/TXT 和事项台账时，在“我的事项”导出栏勾选“附带完整原文”。报告本身仍包含支持其结论的必要原文节选。
- 被引用消息有账号标识时按账号识别；旧记录如果只有相同显示名，会保守归到“可能引用了我”，不能仅凭同名确定是本人。
- 未完成事项仍跨天保留，不会因模型的完成建议被自动关闭。相同原始请求的任务和待回复在报告中合为一张卡，工作台保留原始状态记录。

1. 登录电脑微信，双击“启动微信复盘.cmd”，浏览器会打开本地工作台。
2. 第一次使用进入“数据与模型”，选择正在登录的本人账号。填写本人显示名和群内常用称呼有助于识别点名任务。
3. 如需 AI，填写供应商兼容 Chat Completions 的 Base URL、模型名称、API Key，勾选启用模型分析并保存，点击“测试模型连接”。密钥经 Windows DPAPI 加密，只能由当前 Windows 用户解密。未配置时仍可使用本地规则筛查。
4. 回到“今晚复盘”，选择最近 24/48/72 小时、7/30 天或自定义时间，再点“读取并整理”。首次建议选最近 30 天，补充历史未完成事项。
5. 分别核对待办、待回复和咨询，点击“查看消息依据”回看原文。标记已处理、稍后处理或与我无关；这些状态不会因次日重新读取被覆盖。
6. 点击“导出重点报告”，获得离线 HTML、Markdown、PNG 和 ZIP；完整原文数据为可选项。

这是按需运行的晚间复盘，没有默认创建定时任务，不会自动给联系人发送回复。

## 判断边界

- 待回复不是微信未读状态。程序结合消息方向和上下文提取候选；本地规则尤其需要人工复核。
- “收到”“好的”和后面发过其他消息都不代表相关任务完成。AI 的完成判断只作为建议，手动确认后才关闭事项。
- 群里未指向本人的请求标为“待确认是否与我有关”，避免把别人的工作算到你头上。
- 历史未完成事项跨天保留；不在当前时间窗内的原始证据仍参与分析。没有首次导入的历史记录不会凭空出现。
- 只读取当前电脑同步的普通群聊及私聊，排除公众号与部分系统会话。不会 OCR 或滚动微信窗口。
- 图片、视频和音频内部内容不识别，仅保留可用微信语音转写等已有文本。
- 完整性检查不代表消息已全部同步。界面显示未映射会话表、未知发送者和解析错误数量。
- AI 会按会话和长度分段，超长对话保留相邻重叠上下文，仍可能遗漏跨远距离的关联；状态不自动关闭。
- 切换模型设置不会删除旧记录。重新分析得出的新条目不会自动清除旧待办，请手动核实并关闭误报。

## 文件和隐私

- `settings.local.json`：本机账号和模型设置（不包含明文 API Key）。
- `local-data/credential.dpapi`：加密后的模型密钥。
- `local-data/workbench.sqlite`：本地消息、待办和处理状态，以账号隔离。
- `local-data/reports/`：每次导出的独立目录；报告含聊天原文，分享前请检查。
- `local-data/private/`：临时加密及解密副本，正常完成或异常抛出时自动清理。强制终止可能留有残余，可在程序完全退出后手动清理其中的 `read-*` 子目录。
- 本机服务只监听 `127.0.0.1`，包含 Host/Origin 与操作令牌校验；不公开部署。
- 启用外部模型才会把消息文本及历史待办依据发到你配置的服务；连接测试只发送测试文字。

## 故障排查

无法取得密钥：确认所选目录是当前电脑微信登录的账号，保持登录后重试。客户端升级可能使内存结构变化，需要适配。不要把密钥、完整数据库或私聊记录发到公开 Issue。

无法创建稳定副本：微信正大量同步或更新数据库，请等同步结束后重试。

模型连接失败：检查接口是否支持 `/chat/completions`、Base URL 是否缺少 `/v1`、模型名称和密钥是否正确。失败批次会标明回退本地筛查，不会伪装成成功的 AI 总结。

关闭服务：在启动的终端按 Ctrl+C。重新启动会保留历史消息和处理状态。需要换端口时可运行 `.venv\Scripts\python.exe app.py --port 8766`。

## 实现与验证

新读取器复用上游密钥提取和消息解析，对选定数据库创建文件状态稳定的只读快照，在临时副本上校验 WAL 并只合并已提交事务，再执行 SQLite quick_check。原始数据库不进行写入。

运行离线测试：`.venv\Scripts\python.exe -m unittest discover -s tests -v`。测试样例是虚构数据，测试通过不等于本机微信读取兼容性验证通过。实际验收结果见 `验证记录.md`。


## 2026-09-21：收藏与按人查找

- **收藏**：在事项的消息依据、群聊故事的原文依据，或“按人查找”的消息卡片中，点击“收藏”。进入左侧收藏页，可编辑标题、标签、备注，按关键词查找或取消收藏。重复收藏不会清空备注，取消收藏不删除原始消息。收藏按微信账号隔离并保存在本机，重启后仍保留。
- **按人查找**：输入昵称或微信 ID，先从候选中选择具体身份。同名用户分开显示。可跨群聊和私聊查看该用户的发言、被 @、被引用消息，并按关键词、起止时间和会话筛选。结束时间不包含在范围内。
- 搜索仅覆盖已经导入本项目的记录，需要更长历史时先扩大“读取并整理”的时间范围。旧记录缺少结构化 @ 或引用标识时可能漏检；图片、未转写语音内部文字不会被搜索。
- “历史关联事项”按原文证据关联用户，不等于此人负责；不受消息列表的时间和关键词筛选限制。列表分页加载，避免一次渲染全部消息。
- 收藏管理需要本地工作台；离线导出的报告用于阅读，不会修改收藏库。
