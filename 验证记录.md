# 验证说明

运行测试：`python -m unittest discover -s tests -v`。

本地扩展版已有 42 项自动测试，覆盖消息证据、故事合并、分页摘要、待办状态持久化、账号隔离、收藏与按人查询、WAL 提交边界和接口访问控制。测试使用虚构数据，不包含真实聊天记录。

界面已验证分群故事、按人筛选、收藏编辑和取消收藏，以及 PNG / HTML / Markdown 导出。具体微信版本兼容性仍需在使用者本机验证，测试通过不代表所有微信版本均可读取。

真实账号、聊天数量、读取时间窗口、本机路径和截图均不随仓库发布。
